import fonts from './fonts'
import colors from './colors'
import dimensions from './dimesions'
import { globalStyles } from './styles'
import { textSize,borderRadius } from './typography'

const { width,height,isTablet,Spacing } = dimensions;


export  { colors, fonts, globalStyles, borderRadius, width, height, isTablet, Spacing, textSize }
